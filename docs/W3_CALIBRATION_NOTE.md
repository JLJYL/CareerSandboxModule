# W3 正规化门槛与权重校准记录



日期：2026-08-03  

分支：`feat/a-w3-calibration`



## 校准资料与回归结果



本次使用 `jobs_all.jsonl` 共 4,125 笔职位进行 bge-m3 真向量校准，其中包含 572 个不重复技能字串、5,988 次技能要求，第一段 alias 精确命中 59 项。



原默认门槛 `0.62` 在正式黄金集回归中得到 13/15 格、逐技能 73/75。两笔失败皆将「报表汇整与管理」错误判为 covered：



- `财务报表制作 → 报表制作`，相似度 0.8701

- `业绩管理 → 业绩与管理报表撰写`，相似度 0.7991



两者语意接近但职业能力不同，不应自动合并。将 VocabNormalizer 默认门槛调整为 `0.88` 后，bge-m3 正式黄金集回归恢复为 15/15 格、逐技能 75/75，故门槛定案为 `0.88`。



详细证据见：



- `docs/calibration_report.md`

- `docs/golden_pairs_report.md`



## matchScore 权重裁决



正式回归的 matchScore 矩阵符合预期。含数据经验的 `r_resume-002` 在「数据输入员」栏得分 56，明显高于其他四份履历，满足关键排序检查；其余两栏的排序与分数量级未出现足以支持调整权重的反例。



B 的 HR 案显示，先前异常的根因是骨架真空，并已于上游修复，不是覆盖率与语意分量之间的比例失衡。因此本轮不以权重调整掩盖上游资料问题，matchScore 权重维持覆盖率 `0.65`、语意 `0.35`。



结论：根因是骨架真空、已于上游修复，权重维持。



## 新词条定案

初次校准报告的高频低相似残留候选包括「鼎新」「正航」「Claude」「Zeplin」。本轮将「鼎新／正航」归入 ERP 系统，将「Claude」作为 AI 工具，并将「Zeplin」作为设计协作工具，补入 `tools/build_vocab.py` 的 `SEEDS`。

使用完整的张技能图谱、JL career knowledge 与 jobs_all 资料重跑产生器后，词汇表生成 110 条，MockData tag 覆盖 9/9。最终校准报告的第一段 alias 精确命中为 59/572，低相似高频候选表已无剩余项目。

随后再次执行 bge-m3 正式黄金集回归，结果为 15/15 格、逐技能 75/75；含数据经验的 `r_resume-002` 在「数据输入员」栏以 56 分排名第一，确认新词条未破坏既有判定或关键排序。