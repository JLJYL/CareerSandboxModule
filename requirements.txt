fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pytest>=8.0
httpx>=0.27
# --- Phase 2 deps (do not install yet; keep the skeleton light) ---
# chromadb
# FlagEmbedding        # bge-m3 embeddings
# openai               # or any OpenAI-compatible SDK