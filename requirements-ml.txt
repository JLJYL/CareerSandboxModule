# Local dev only - CI skips this file (torch is huge; model comes from HF cache)
# Deliberately NOT using FlagEmbedding (dtype rename clash with transformers 4.x)
# Do NOT install sentencepiece (native ext crashes on Windows; uninstall if present)
torch>=2.2
transformers>=4.44,<5
huggingface_hub<1.0